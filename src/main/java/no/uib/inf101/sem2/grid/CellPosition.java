package no.uib.inf101.sem2.grid;

public record CellPosition(int row, int col) {
    /**
    
    The CellPosition class is a record that represents the position of a cell in a grid.
    It has two fields, row and col, that represent the row and column numbers of the cell, respectively.
    */
        
}
    